# MIDI -> Discord Mixer dependencies
# Tested on Python 3.10–3.12, Windows 10/11.

PySide6>=6.6
mido>=1.3
python-rtmidi>=1.5
sounddevice>=0.4.6
soundfile>=0.12
numpy>=1.24

# Build-time only (for packaging the .exe):
# pyinstaller>=6.0
